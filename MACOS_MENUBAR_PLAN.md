# macOS 菜单栏模式提案

> 状态：尚未实现，属于后续产品方案。当前 TransLive 是标准桌面 App，悬浮字幕窗口可以独立显示，但没有菜单栏常驻入口。

## 目标

让同传在课堂、会议和视频播放期间更容易常驻，同时保持主要操作可见、退出路径明确，并避免后台持续占用模型资源。

## 推荐交互

菜单栏图标的一级菜单只放常用动作：

- 打开 TransLive
- 开始/停止同传
- 显示/隐藏字幕
- 音频来源：麦克风 / 系统音频
- 字幕模式：双语 / 仅译文
- 当前模型与设备状态
- 退出

设置、术语表、历史记录、全文翻译和模型下载继续留在主窗口，避免菜单过载。

## 窗口策略

- 主窗口关闭时默认隐藏，而不是直接结束同传。
- 用户选择“退出”时，停止音频采集、WebSocket、ASR/MT 任务和 Uvicorn，再终止进程。
- 字幕窗口继续独立置顶；点击穿透只作用于字幕显示区域，工具栏仍可操作。
- 开机启动应由用户显式启用，默认关闭。

## 资源策略

- 未开始同传时不运行 VAD 或推理循环。
- 可提供“释放模型内存”动作，而不是每次隐藏窗口就卸载模型。
- App Nap 策略只在同传或下载进行时保持活动，空闲时恢复系统节能行为。
- 菜单栏状态只读取已有健康数据，避免额外高频轮询。

## 技术路线

当前 PyWebView 壳可以通过 PyObjC 创建 `NSStatusItem`，并将菜单动作转发给现有桌面控制器。实现前需要先把启动、窗口显示、采集状态和退出流程收敛成单一状态机，避免主窗口、字幕窗和菜单栏分别维护一套状态。

如果未来改为原生 Swift/AppKit 壳，可以保留 FastAPI/模型进程作为受控子服务，但这属于更大的架构升级，不应与首版菜单栏功能同时进行。

## 验收条件

- 菜单栏动作与主界面状态始终一致。
- 麦克风和系统音频都能从菜单栏启动、停止并再次启动。
- 隐藏主窗口不影响字幕；退出后没有残留进程。
- 空闲状态不持续占用明显 CPU。
- macOS 14 和 15 上分别验证权限、全屏字幕、Dock 退出与登录项行为。
